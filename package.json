{
  "name": "dota-database",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc -p ./configs/tsconfig.json --pretty -d",
    "migration:generate": "drizzle-kit generate:pg --config=./configs/drizzle.config.ts",
    "migration:push": "node --loader esbuild-register/loader -r esbuild-register src/db/migrate.ts"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "drizzle-orm": "^0.29.0",
    "fs": "^0.0.1-security",
    "pg": "^8.11.3",
    "typescript": "^5.2.2"
  },
  "devDependencies": {
    "drizzle-kit": "^0.20.1",
    "esbuild": "^0.19.5",
    "esbuild-register": "^3.5.0"
  }
}
